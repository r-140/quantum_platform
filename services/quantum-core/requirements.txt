# Runtime dependencies (qiskit, qiskit-aer, scipy) are now declared in
# pyproject.toml's [project.dependencies] -- that's what makes them
# transitively installed when another service does
# `pip install -e ../quantum-core` (see services/api/requirements.txt).
# This file now only pins dev/test tooling not needed by dependents.

pytest>=8.0
pytest-asyncio>=0.23