# Runtime dependencies for the API service.
#
# quantum-core is installed as an editable local package (not from PyPI --
# it's this project's own library, see services/quantum-core/). Path is
# relative to this file's directory.
-e ../quantum-core

fastapi>=0.110
uvicorn[standard]>=0.29

# Dev/test
pytest>=8.0
httpx>=0.27   # required by FastAPI's TestClient